(()=>{
  const CMS_KEY='untozCommandCMS';
  const defaults=[
    {id:'news',name:'Untoz News',short:'News',accent:'#ff453a',tagline:'The world, as it happens.',description:'Breaking stories, explainers and updates from across the world.',categories:['News'],hero:'https://images.unsplash.com/photo-1504711434969-e33886168f5c?auto=format&fit=crop&w=1800&q=88',navigation:['Latest','World','Technology','Live'],enabled:true},
    {id:'sports',name:'Untoz Sports',short:'Sports',accent:'#0cdb46',tagline:'Every game. Every moment.',description:'Scores, stories, live coverage and the biggest moments in sport.',categories:['Sports'],hero:'https://images.unsplash.com/photo-1461896836934-ffe607ba8211?auto=format&fit=crop&w=1800&q=88',navigation:['Football','Live','Results','Features'],enabled:true},
    {id:'gaming',name:'Untoz Gaming',short:'Gaming',accent:'#8b5cf6',tagline:'Play what’s next.',description:'Games, reveals, reviews, esports and everything worth playing.',categories:['Gaming'],hero:'https://images.unsplash.com/photo-1542751371-adc38448a05e?auto=format&fit=crop&w=1800&q=88',navigation:['News','Reviews','Trailers','Esports'],enabled:true},
    {id:'space',name:'Untoz Space',short:'Space',accent:'#3b5bff',tagline:'Beyond our world.',description:'Missions, astronomy, discoveries and humanity’s journey into space.',categories:['Space'],hero:'https://images.unsplash.com/photo-1446776811953-b23d57bd21aa?auto=format&fit=crop&w=1800&q=88',navigation:['Missions','Astronomy','Launches','Live'],enabled:true},
    {id:'pop',name:'Untoz Pop',short:'Pop',accent:'#ff2e88',tagline:'Culture moves fast.',description:'Music, entertainment, internet culture and the moments everyone is talking about.',categories:['Entertainment','Music'],hero:'https://images.unsplash.com/photo-1492684223066-81342ee5ff30?auto=format&fit=crop&w=1800&q=88',navigation:['Music','Entertainment','Culture','Trending'],enabled:true},
    {id:'kids',name:'Untoz Kids',short:'Kids',accent:'#ff9f0a',tagline:'Play. Discover. Imagine.',description:'Shows, stories, learning and safe fun made for younger audiences.',categories:['Kids'],hero:'https://images.unsplash.com/photo-1503454537195-1dcabb73ffb9?auto=format&fit=crop&w=1800&q=88',navigation:['Shows','Play','Learn','Characters'],enabled:true},
    {id:'archives',name:'Untoz Archives',short:'Archives',accent:'#b78a4a',tagline:'Stories worth keeping.',description:'Restored media, historic collections and the memory of Untoz.',categories:['Archives'],hero:'https://images.unsplash.com/photo-1481627834876-b7833e8f5570?auto=format&fit=crop&w=1800&q=88',navigation:['Collections','Restored','History','Discover'],enabled:true}
  ];
  let open=false,activeId='',draft=null;
  const clone=v=>JSON.parse(JSON.stringify(v));
  const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  const slugify=v=>String(v||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,'-').replace(/^-+|-+$/g,'');
  function read(){try{return JSON.parse(localStorage.getItem(CMS_KEY)||'{}')}catch{return {}}}
  function write(state){localStorage.setItem(CMS_KEY,JSON.stringify(state))}
  function list(){const state=read();return Array.isArray(state.brands)&&state.brands.length?state.brands:defaults}
  function ensure(){const state=read();if(!Array.isArray(state.brands)||!state.brands.length){state.brands=clone(defaults);write(state)}return state}
  function toast(msg){let el=document.getElementById('brands-toast');if(!el){el=document.createElement('div');el.id='brands-toast';el.className='brands-toast';document.body.appendChild(el)}el.textContent=msg;el.classList.add('show');setTimeout(()=>el.classList.remove('show'),2600)}
  function installNav(){const nav=document.querySelector('.nav');if(!nav)return;let b=nav.querySelector('[data-command-brands]');if(!b){b=document.createElement('button');b.dataset.commandBrands='1';b.textContent='Brands';b.onclick=openBrands;const publishing=nav.querySelector('[data-command-publishing]');publishing?nav.insertBefore(b,publishing):nav.appendChild(b)}b.classList.toggle('active',open)}
  function openBrands(){open=true;ensure();const brands=list();if(!activeId||!brands.some(x=>x.id===activeId))activeId=brands[0]?.id||'';draft=clone(brands.find(x=>x.id===activeId)||null);document.querySelectorAll('.nav button').forEach(x=>x.classList.remove('active'));document.querySelector('[data-command-brands]')?.classList.add('active');render()}
  function leave(){open=false;document.querySelector('[data-command-brands]')?.classList.remove('active')}
  function brandSiteUrl(id){return new URL(`../${slugify(id)}/`,location.href).href}
  function markup(){const brands=list();const brand=draft||brands.find(x=>x.id===activeId);if(!brand)return '<div class="brands-empty">No brands configured.</div>';return `<section class="brands-manager"><aside class="brands-sidebar"><div class="brands-sidebar-head"><h2>Brands</h2><button class="brands-new" data-brand-new>+ New</button></div><div class="brands-list">${brands.map(b=>`<button class="brand-list-item ${b.id===activeId?'active':''}" data-brand-select="${esc(b.id)}"><span class="brand-list-swatch" style="background:${esc(b.accent||'#174cff')}"></span><span class="brand-list-copy"><b>${esc(b.name||b.id)}</b><small>${b.enabled===false?'Hidden':`/${esc(b.id)}/`}</small></span></button>`).join('')}</div><div class="brands-help">Each brand uses the Untoz network layout but keeps its own identity, content categories and navigation. Changes go live with the normal Publish workflow.</div></aside><div class="brands-editor"><div class="brands-editor-head"><div><span class="eyebrow">BRAND EDITOR</span><h2>${esc(brand.name||'Untitled brand')}</h2></div><div class="brands-editor-actions"><a href="${brandSiteUrl(brand.id)}" target="_blank">↗ Open site</a><button data-brand-duplicate>Duplicate</button><button class="danger" data-brand-delete>Delete</button><button class="primary" data-brand-save>Save brand</button></div></div><div class="brands-layout"><div class="brands-fields"><div class="brand-field"><label>Brand name</label><input data-brand-field="name" value="${esc(brand.name||'')}"></div><div class="brand-field"><label>Short name</label><input data-brand-field="short" value="${esc(brand.short||'')}"></div><div class="brand-field"><label>Route ID</label><input value="${esc(brand.id||'')}" readonly><div class="brands-slug-note">Public route: /${esc(brand.id||'')}/</div></div><div class="brand-field"><label>Accent colour</label><div class="brand-color-row"><input type="color" data-brand-color value="${esc(/^#[0-9a-f]{6}$/i.test(brand.accent||'')?brand.accent:'#174cff')}"><input data-brand-field="accent" value="${esc(brand.accent||'#174cff')}"></div></div><div class="brand-field full"><label>Tagline</label><input data-brand-field="tagline" value="${esc(brand.tagline||'')}"></div><div class="brand-field full"><label>Description</label><textarea data-brand-field="description">${esc(brand.description||'')}</textarea></div><div class="brand-field full"><label>Hero image URL</label><input data-brand-field="hero" value="${esc(brand.hero||'')}" placeholder="https://…"></div><div class="brand-field full"><label>Content categories</label><input data-brand-list="categories" value="${esc((brand.categories||[]).join(', '))}" placeholder="News, Technology"><div class="brands-slug-note">Posts matching any of these categories feed this brand.</div></div><div class="brand-field full"><label>Navigation</label><input data-brand-list="navigation" value="${esc((brand.navigation||[]).join(', '))}" placeholder="Latest, Live, Features"><div class="brands-slug-note">Comma-separated navigation labels shown in the brand header.</div></div><label class="brand-toggle full"><input type="checkbox" data-brand-enabled ${brand.enabled!==false?'checked':''}> Brand enabled and visible in the Untoz network</label></div><div class="brands-preview-wrap"><div class="brands-preview-label">LIVE PREVIEW</div><div class="brands-preview" style="--brand-preview:${esc(brand.accent||'#174cff')}"><div class="brands-preview-header"><strong>untoz</strong><i></i><span>${esc(brand.short||'Brand')}</span></div><div class="brands-preview-hero">${brand.hero?`<img src="${esc(brand.hero)}" alt="">`:''}<div class="brands-preview-shade"></div><div class="brands-preview-copy"><small>${esc((brand.name||'Untoz Brand').toUpperCase())}</small><h3>${esc(brand.tagline||'Brand tagline')}</h3><p>${esc(brand.description||'Brand description')}</p></div></div><div class="brands-preview-nav">${(brand.navigation||[]).map(x=>`<span>${esc(x)}</span>`).join('')}</div></div></div></div></div></section>`}
  function render(){if(!open)return;const view=document.querySelector('.view.active');const title=document.querySelector('.topbar h1');if(!view||!title)return;title.textContent='Brands';view.innerHTML=markup();bind();installNav()}
  function refreshPreviewFromDOM(){if(!draft)return;document.querySelectorAll('[data-brand-field]').forEach(el=>draft[el.dataset.brandField]=el.value);document.querySelectorAll('[data-brand-list]').forEach(el=>draft[el.dataset.brandList]=el.value.split(',').map(x=>x.trim()).filter(Boolean));draft.enabled=document.querySelector('[data-brand-enabled]')?.checked!==false;const color=document.querySelector('[data-brand-color]');if(color&&document.activeElement===color)draft.accent=color.value;render()}
  function save(){if(!draft)return;const state=ensure();const idx=state.brands.findIndex(x=>x.id===activeId);if(idx<0)return;draft.categories=Array.isArray(draft.categories)?draft.categories:[];draft.navigation=Array.isArray(draft.navigation)?draft.navigation:[];state.brands[idx]=clone(draft);write(state);window.dispatchEvent(new CustomEvent('untoz:brand-saved',{detail:{id:draft.id,name:draft.name}}));toast(`${draft.name||'Brand'} saved locally.`);render()}
  function createBrand(){const name=prompt('New Untoz brand name:','Untoz New');if(!name)return;const state=ensure();let id=slugify(name.replace(/^untoz\s+/i,''))||`brand-${Date.now()}`;let n=2,baseId=id;while(state.brands.some(x=>x.id===id))id=`${baseId}-${n++}`;const b={id,name,short:name.replace(/^Untoz\s+/i,'')||'New',accent:'#1f6ffa',tagline:'A new world in the Untoz universe.',description:'Stories and experiences from Untoz.',categories:[],hero:'',navigation:['Latest','Features','Live'],enabled:true};state.brands.push(b);write(state);activeId=id;draft=clone(b);window.dispatchEvent(new CustomEvent('untoz:brand-saved',{detail:{id,name,created:true}}));toast('New brand created.');render()}
  function duplicate(){if(!draft)return;const state=ensure();let baseId=`${draft.id}-copy`,id=baseId,n=2;while(state.brands.some(x=>x.id===id))id=`${baseId}-${n++}`;const b={...clone(draft),id,name:`${draft.name} Copy`,short:`${draft.short} Copy`};state.brands.push(b);write(state);activeId=id;draft=clone(b);window.dispatchEvent(new CustomEvent('untoz:brand-saved',{detail:{id,name:b.name,created:true}}));toast('Brand duplicated.');render()}
  function remove(){if(!draft||!confirm(`Delete ${draft.name}? Existing published route files are not removed until route cleanup is added.`))return;const state=ensure();state.brands=state.brands.filter(x=>x.id!==activeId);write(state);const next=state.brands[0];activeId=next?.id||'';draft=clone(next||null);window.dispatchEvent(new CustomEvent('untoz:brand-saved',{detail:{deleted:true}}));toast('Brand removed from CMS.');render()}
  function bind(){document.querySelectorAll('[data-brand-select]').forEach(b=>b.onclick=()=>{activeId=b.dataset.brandSelect;draft=clone(list().find(x=>x.id===activeId));render()});document.querySelector('[data-brand-new]')?.addEventListener('click',createBrand);document.querySelector('[data-brand-save]')?.addEventListener('click',save);document.querySelector('[data-brand-duplicate]')?.addEventListener('click',duplicate);document.querySelector('[data-brand-delete]')?.addEventListener('click',remove);document.querySelectorAll('[data-brand-field],[data-brand-list],[data-brand-enabled]').forEach(el=>el.addEventListener('change',refreshPreviewFromDOM));document.querySelectorAll('[data-brand-field],[data-brand-list]').forEach(el=>el.addEventListener('blur',refreshPreviewFromDOM));const color=document.querySelector('[data-brand-color]');if(color)color.addEventListener('input',()=>{const text=document.querySelector('[data-brand-field="accent"]');if(text)text.value=color.value;draft.accent=color.value;render()})}
  function watchNavigation(){document.addEventListener('click',e=>{if(e.target.closest('.nav [data-view],.nav [data-command-publishing]'))leave()},true)}
  function enhance(){installNav();if(open)render()}
  function install(){ensure();watchNavigation();const obs=new MutationObserver(()=>requestAnimationFrame(()=>installNav()));obs.observe(document.documentElement,{childList:true,subtree:true});installNav()}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();
